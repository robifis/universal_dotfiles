# ~/.config/wofi/config

# Mode: (drun, run, dmenu, window, etc.)
# Set via command line usually, e.g., --show drun
# mode=drun

# General Appearance
width=40%           # Percentage of screen width, or pixels (e.g., 500)
height=50%          # Percentage of screen height, or pixels (e.g., 400)
# location=center   # center, top, bottom, left, right, top-left, etc.
# anchor=center     # If location is a corner, anchor defines which part of wofi attaches there
# xoffset=0
# yoffset=0
allow_images=true   # Show application icons (if your icon theme provides them)
image_size=24       # Size of application icons in pixels
# term=kitty        # Terminal to use for actions requiring one (if not default)
# sort_order=default # alphabetical, default (mru)
# filter_rate=100   # Milliseconds between filtering keystrokes
# ignore_case=true  # Case-insensitive search
# hide_scroll=false # Show scrollbar
# matching=fuzzy    # Normal, fuzzy

# Style (points to our CSS file)
# style=~/.config/wofi/style.css # Wofi usually finds this automatically if in the default location

# Colors can be set here too, but CSS is more flexible.
# Example:
# background_color=#282828FF # RGBA hex
# text_color=#EBDBB2FF
# border_color=#665C54FF

# Options for specific modes
# [drun]
# display_generic=true
